import { View, Text, Button } from "react-native";

export default function VersionInfo({ navigation }) {
    return (
        <View>
            <Text>버전 정보</Text>

           
        </View>
    );
}

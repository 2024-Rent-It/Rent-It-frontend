import { View, Text, Button } from "react-native";

export default function TermsOfService({ navigation }) {
    return (
        <View>
            <Text>이용약관</Text>

           
        </View>
    );
}

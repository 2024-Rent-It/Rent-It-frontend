import { View, Text, Button } from "react-native";

export default function LocationService({ navigation }) {
    return (
        <View>
            <Text>위치기반</Text>

           
        </View>
    );
}
